<%= form_with(model: agreement, local: true) do |form| %>
  <% if agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agreement.errors.count, "error") %> prohibited this agreement from being saved:</h2>

      <ul>
      <% agreement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date, id: :agreement_date %>
  </div>

  <div class="field">
    <%= form.label :evaluation_id_id %>
    <%= form.text_field :evaluation_id_id, id: :agreement_evaluation_id_id %>
  </div>

  <div class="field">
    <%= form.label :user_id_id %>
    <%= form.text_field :user_id_id, id: :agreement_user_id_id %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.number_field :rate, id: :agreement_rate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
